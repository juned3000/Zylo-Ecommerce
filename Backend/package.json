{
  "name": "zylo-ecommerce-backend",
  "version": "1.0.0",
  "description": "Backend API for Zylo Ecommerce platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node -e \"require('dotenv').config(); require('./routes/products').post('/seed', null, { json: (data) => console.log('Seeded:', data) })\"",
    "diagnostics": "node ../debug-products.js",
    "health": "curl -s http://localhost:5000/api/health | jq .",
    "db-check": "curl -s http://localhost:5000/api/diagnostics/database | jq ."
  },
  "keywords": ["ecommerce", "api", "nodejs", "express", "mongodb"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
